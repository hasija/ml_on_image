{
 "update": {
    "labels": [
      {
        "add": "{{label}}"
      }
    ]
  },
  "fields": {
    "summary": "{{summary}}",
    "issuetype": {
      "name": "{{issue_type}}"
    },
    "description": "{{description}}",
    "reporter": {
      "emailAddress": "{{reporter}}"
    },
    "priority": {
      "name": "{{priority}}"
    },
    "assignee": {
      "name": "{{assignee}}"
    }

  }
}
